name: main

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: ['8.4']
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}

      - name: Validate composer.json and composer.lock
        run: composer validate --strict --no-plugins

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-plugins

      - name: Run test suite with coverage
        run: XDEBUG_MODE=coverage vendor/bin/codecept run unit --coverage-xml

      - name: Download Scrutinizer ocular.phar
        run: composer require uma/ocular -W --prefer-dist --no-progress --no-plugins

      - name: Upload coverage
        run: vendor/bin/ocular code-coverage:upload --format=php-clover tests/_output/coverage.xml
